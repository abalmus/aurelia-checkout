export class SectionCompleted {
    constructor(section) {
        this.section = section;
    }
}

export class EditSection {
    constructor(section) {
        this.section = section;
    }
}

export class SectionUpdated {
    constructor(section) {
        this.section = section;
    }
}
